{
  "name": "zimserver",
  "version": "1.0.0",
  "description": "Socket Server to Run ZIM Socket",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": { 
    "express": "^4.17.1",
    "socket.io": "^4.0.0"
  }
}
